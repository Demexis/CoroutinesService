using UnityEngine;

namespace CustomCoroutines {
    // Used to use basic MonoBehaviour methods.
    // Should not contain any logic.
    internal sealed class EmptyMonoBehaviour : MonoBehaviour { }
}